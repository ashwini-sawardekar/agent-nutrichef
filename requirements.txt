# requirements.txt

# --- Google Agent Development Kit (ADK) ---
# Replace 'google-genai-adk' with the official package name provided in your course.
google-genai-adk==1.0.0 

# --- Core Python Packages ---
# Used for data manipulation, context handling, and structured I/O
pydantic>=2.0.0
typer>=0.9.0

# --- LLM Provider Dependencies ---
# Needed if you are using the official Google GenAI SDK directly, 
# even if the ADK abstracts some of it.
google-genai>=0.5.0

# --- Development & Utility (Optional but Recommended) ---
# Used for environment management and code quality
python-dotenv>=1.0.0
black==23.1.0 
isort==5.12.0